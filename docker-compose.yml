# FOR CI/DEPLOYMENT ONLY — not required for local dev
version: "3.9"
services:
  db:
    image: postgres:15
    environment:
      POSTGRES_DB: docketvault
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

  redis:
    image: redis:7
    ports:
      - "6379:6379"

volumes:
  pgdata:
